interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate maintainer queues and worker threads"
  default_prompt: "Use $maintainer-orchestrator to coordinate bounded issue/PR lanes, mini-orchestrator owners, worker threads, owner decisions, CI, and release readiness. Preserve existing work, refill only after terminal state, and ask the owner only after autonomous work is exhausted."
