interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate maintainer queues and worker threads"
  default_prompt: "Use $maintainer-orchestrator to coordinate repository queues, worker threads, owner decisions, CI, and release readiness. Keep orchestration separate from implementation, preserve existing work, assign one coherent next action per repository, and ask the owner only after autonomous work is exhausted."
