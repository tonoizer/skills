---
description: Turn an investigation or codebase question into a grounded interactive HTML guide.
argument-hint: <topic, investigation, or scope>
---

Use `$readout`.

Topic: $ARGUMENTS

Create a durable self-contained HTML readout, verify code-specific claims against exact source revisions, validate the artifact, and return its absolute path.
