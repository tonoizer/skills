---
description: Reproduce and fix a bug with the smallest high-confidence patch.
argument-hint: <bug description, issue, or failing command>
---

Use `$bug`.

Investigate: $ARGUMENTS

Follow the repo `AGENTS.md`. Reproduce first, identify the cause, make the
smallest safe fix, run relevant tests, and stop if this requires a larger
refactor or product decision.
