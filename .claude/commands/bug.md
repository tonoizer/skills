---
description: Reproduce and fix a bug with the smallest high-confidence patch.
argument-hint: <bug description, issue, or failing command>
---

Use `$debug`.

Investigate: $ARGUMENTS

Follow the repo `AGENTS.md`. Use the focused debug-subagent workflow, reproduce
first, prove the cause, make the smallest safe fix, and run relevant tests.
