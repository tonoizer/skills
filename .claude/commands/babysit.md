---
description: Keep a PR merge-ready by handling comments, clear conflicts, and CI in a loop.
argument-hint: <pr number or url>
---

Use `$babysit`.

PR: $ARGUMENTS

Stay with the PR until its current head is merge-ready or a precise blocker requires user input. Do not merge unless explicitly requested.
