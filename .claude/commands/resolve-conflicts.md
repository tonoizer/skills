---
description: Resolve and finish an in-progress conflicted Git operation safely.
argument-hint: <optional merge, rebase, cherry-pick, or revert context>
---

Use `$resolve-conflicts`.

Context: $ARGUMENTS

Trace both sides' intent, resolve every hunk without silent loss, verify the result, and finish the active Git operation.
